// default style for an article
// start with a single column grid
// top children have named grid areas

article.{{ component.name|hyphenize }} {

  --article-color: var(--color-fg);
  --article-bg: var(--color-bg);

  --article-hover-color: var(--color-bg);
  --article-hover-bg: var(--color-fg);

}
